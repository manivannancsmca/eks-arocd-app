name: EKS Argocd

on:
  push:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Code Checkout
      uses: actions/checkout@v4

    - name: Build the Docker image
      run: "docker build -t manivannancsmca/eks-arocd-app:$GITHUB_SHA ."
